apply plugin: 'common-kotlin-jvm'

dependencies {
    compile project(":common:common-thread")

    compile deps.kotlinStdLib
    compile deps.javaxInject
    compile deps.coroutines

    testCompile project(":common:common-test")
}